[project]
name = "postgres-documentation-explore"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "polars>=1.44.2",
]

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
]
